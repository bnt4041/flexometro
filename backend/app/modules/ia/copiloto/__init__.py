"""Copiloto: el chat que acompaña a toda la aplicación.

Se apoya en lo que ya hay en vez de duplicarlo: la wiki de `soporte` para
responder cómo se hace algo, el motor de `informes` para las cifras, los
destinos de `importador` para crear, y el registro de módulos para saber
dónde está cada pantalla. Lo propio de aquí es el registro de herramientas
con permisos (`registro.py`) y la regla de que escribir siempre pasa por una
confirmación (`ejecutores.py`).
"""
